[package]
name = "locus-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
locus-parser = { path = "../locus-parser" }
spaces-core = { path = "../spaces-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
